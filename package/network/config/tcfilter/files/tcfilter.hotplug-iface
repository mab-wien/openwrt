# SPDX-License-Identifier: GPL-2.0-only
# clsact filters are lost when the network is reconfigured; re-apply on ifup.

[ "$ACTION" = "ifup" ] || return 0
[ -x /etc/init.d/tcfilter ] || return 0

/etc/init.d/tcfilter reload
