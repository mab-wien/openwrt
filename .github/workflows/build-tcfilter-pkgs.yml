# Fork-local CI. Not part of upstream OpenWrt.
#
# Builds the tcfilter + luci-app-tcfilter packages (and their tc-full
# dependency) for realtek/rtl930x and publishes the .apk files to a rolling
# pre-release tagged `tcfilter-pkgs-latest`, so they can be installed on the
# switch without a GitHub login, e.g.:
#
#   apk add --allow-untrusted \
#     https://github.com/mab-wien/openwrt/releases/download/tcfilter-pkgs-latest/tcfilter-1-r1.apk \
#     https://github.com/mab-wien/openwrt/releases/download/tcfilter-pkgs-latest/luci-app-tcfilter-0.apk
#
# (--allow-untrusted because the fork signs with its own throwaway key.)

name: Build tcfilter packages

on:
  push:
    branches: [package-tcfilter]
    paths:
      - 'package/network/config/tcfilter/**'
      - 'package/luci-app-tcfilter/**'
      - '.github/workflows/build-tcfilter-pkgs.yml'
  workflow_dispatch:

concurrency:
  group: build-tcfilter-pkgs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    permissions:
      contents: write
    steps:
      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc \
            /opt/hostedtoolcache/CodeQL /usr/local/share/boost \
            "$AGENT_TOOLSDIRECTORY"
          sudo docker image prune --all --force || true
          df -h /

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential clang flex bison g++ gawk gcc-multilib \
            g++-multilib gettext git libncurses-dev libssl-dev \
            python3-setuptools rsync swig unzip zlib1g-dev file wget \
            libelf-dev ecj fastjar java-propose-classpath

      - uses: actions/checkout@v4

      - name: Update and install feeds
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Configure realtek/rtl930x (no device profile - packages only)
        run: |
          cat > .config <<'EOF'
          CONFIG_TARGET_realtek=y
          CONFIG_TARGET_realtek_rtl930x=y
          CONFIG_PACKAGE_tcfilter=y
          CONFIG_PACKAGE_luci-app-tcfilter=y
          EOF
          make defconfig
          grep -E 'CONFIG_PACKAGE_(tcfilter|luci-app-tcfilter|tc-full)=|CONFIG_TARGET_ARCH_PACKAGES=' .config

      - name: Download sources
        run: make -j$(nproc) download

      - name: Build toolchain
        run: |
          make -j$(nproc) tools/install toolchain/install || {
            echo "::group::verbose single-threaded retry"; make -j1 V=s tools/install toolchain/install; echo "::endgroup::"; exit 1;
          }

      - name: Build packages
        run: |
          set -e
          # iproute2 -> tc-full (tcfilter's runtime dep); the luci-base stack
          # is pulled in automatically by luci-app-tcfilter/compile.
          pkgs="package/network/utils/iproute2/compile \
                package/network/config/tcfilter/compile \
                package/luci-app-tcfilter/compile"
          make -j$(nproc) $pkgs || {
            echo "::group::verbose single-threaded retry"; make -j1 V=s $pkgs; echo "::endgroup::"; exit 1;
          }

      - name: Collect artifacts
        id: collect
        run: |
          arch=$(sed -n 's/^CONFIG_TARGET_ARCH_PACKAGES="\(.*\)"/\1/p' .config)
          src="bin/packages/${arch}/base"
          mkdir -p out
          cp "$src"/tcfilter-*.apk "$src"/luci-app-tcfilter-*.apk "$src"/tc-full-*.apk out/
          ( cd out && sha256sum *.apk | tee sha256sums )
          ls -lah out
          echo "arch=$arch" >> "$GITHUB_OUTPUT"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tcfilter-packages-${{ steps.collect.outputs.arch }}-${{ github.sha }}
          path: out/*
          if-no-files-found: error
          retention-days: 30

      - name: Refresh the "latest" package release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: tcfilter-pkgs-latest
        run: |
          built_sha=$(git rev-parse HEAD)
          git push -f origin "${built_sha}:refs/tags/${TAG}"
          gh release delete "$TAG" -R "$GITHUB_REPOSITORY" --yes || true
          gh release create "$TAG" -R "$GITHUB_REPOSITORY" --verify-tag \
            --title "tcfilter packages - latest" \
            --notes "Automated build of \`package-tcfilter\` @ ${built_sha} (run ${GITHUB_RUN_ID}) for realtek/rtl930x (${{ steps.collect.outputs.arch }}). Install with \`apk add --allow-untrusted <url>\`." \
            --prerelease \
            out/*
